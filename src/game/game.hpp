#ifndef GAME_HPP
#define GAME_HPP

namespace vulkat {
	class Game final {
	public:
		Game();
		~Game();

		Game(const Game& other) = delete;
		Game& operator=(const Game& other) = delete;

	private:

	}
}

#endif // GAME_HPP
